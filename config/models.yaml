vision_caption:
  backend: "sambanova"                # "gemini" / "sambanova" / "local" / "dummy"
  model_name: "Llama-4-Maverick-17B-128E-Instruct"   # SambaNova (Llama) モデル
  type: "vision"
  # Gemini を使う場合の例:
  # backend: "gemini"
  # model_name: "gemini-2.5-flash-lite"

diary_writer:
  backend: "sambanova"
  model_name: "Llama-4-Maverick-17B-128E-Instruct"     # SambaNova (Llama) モデル
  type: "text"
  # Gemini を使う場合の例:
  # backend: "gemini"
  # model_name: "gemini-2.5-flash-lite"

scorer:
  backend: "dummy"                 # とりあえずダミーのまま
  model_name: "dummy-scorer"
  type: "text"
